# 服务器部署配置。复制为 .env 后务必改掉下面三个密钥/口令。
# docker compose 会读取本文件。

# 数据库密码(compose 里的 db 服务和 DATABASE_URL 都用它)
POSTGRES_PASSWORD=change-me-strong-password

# 单用户鉴权 token,前端请求头 Authorization: Bearer <此值>
AUTH_TOKEN=change-me-random-token

# OpenRouter
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxx
# 自动处理/OCR 默认模型(也可在「我的」页运行时切换,不用重启)
VISION_MODEL=openai/gpt-4.1-mini
# 问问 AI 默认模型(缺省同上;建议在「我的」里换更强的)
# INSIGHT_MODEL=openai/gpt-4.1
# 自动分类模型默认沿用 VISION_MODEL；运行后可在「AI 设置」写入数据库覆盖。
# 每日 Vision 调用上限。0 = 不限制(交给 API 侧限流,推荐)
VISION_DAILY_BUDGET=0
# 三条 AI 管线共享这一个进程内每日计数。0 表示不限制。
